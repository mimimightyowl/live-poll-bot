# Poll App - Public Voting Interface

Веб-приложение для публичного голосования в опросах с отображением результатов в реальном времени.

## Функционал

- ✅ Просмотр списка доступных опросов
- ✅ Голосование в опросах
- ✅ Отображение результатов в реальном времени через WebSocket
- ✅ Автоматическое переподключение при потере соединения
- ✅ Сохранение факта голосования в localStorage

## Структура проекта

```
poll-app/
├── src/
│   ├── main.ts                    # Точка входа приложения
│   ├── App.vue                    # Корневой компонент
│   ├── components/
│   │   ├── PollsList.vue          # Список опросов
│   │   ├── VotingInterface.vue    # Интерфейс голосования
│   │   └── LiveResults.vue        # Живые результаты с WebSocket
│   ├── views/
│   │   ├── PollsListView.vue      # Страница списка опросов
│   │   └── PollVotingView.vue     # Страница голосования
│   ├── router/
│   │   └── index.ts               # Конфигурация маршрутизации
│   ├── api/
│   │   ├── polls.ts               # API для работы с опросами
│   │   └── votes.ts               # API для отправки голосов
│   └── composables/
│       └── useWebSocket.ts        # WebSocket composable
└── ...
```

## Маршруты

- `/` - Список всех опросов
- `/poll/:id/vote` - Страница голосования в конкретном опросе

## Компоненты

### PollsList.vue

Отображает список доступных опросов с кнопкой "Start Vote".

**Props:**

- `polls: Poll[]` - массив опросов

**Events:**

- `start-vote: (id: number)` - событие начала голосования

### VotingInterface.vue

Интерфейс для выбора варианта ответа и отправки голоса.

**Props:**

- `poll: PollWithOptions` - опрос с вариантами ответов
- `userId: number` - ID пользователя

**Events:**

- `voted: ()` - событие успешного голосования

### LiveResults.vue

Отображает результаты голосования в реальном времени с использованием WebSocket.

**Props:**

- `pollId: number` - ID опроса
- `autoRefresh?: boolean` - автоматическое обновление

**Функции:**

- Подключение к WebSocket
- Автоматическое обновление при получении событий
- Отображение индикатора подключения
- Визуализация результатов с процентами

## API

### polls.ts

```typescript
pollsApi.getAll(): Promise<Poll[]>              // Получить все опросы
pollsApi.getById(id): Promise<Poll>             // Получить опрос по ID
pollsApi.getWithOptions(id): Promise<...>       // Получить опрос с вариантами
pollsApi.getOptions(pollId): Promise<...>       // Получить варианты ответов
pollsApi.getResults(pollId): Promise<...>       // Получить результаты
```

### votes.ts

```typescript
votesApi.create(data: CreateVoteDto): Promise<Vote>  // Создать голос
```

## WebSocket Composable

### useWebSocket.ts

Composable для работы с WebSocket-соединением к realtime-service.

**Опции:**

- `pollId?: number` - ID опроса для автоматической подписки
- `onUpdate?: (results: PollResults) => void` - callback при обновлении
- `onError?: (error: Event) => void` - callback при ошибке
- `reconnectInterval?: number` - интервал переподключения (по умолчанию 3000ms)

**Возвращаемые значения:**

- `isConnected: Ref<boolean>` - статус подключения
- `isReconnecting: Ref<boolean>` - статус переподключения
- `error: Ref<string | null>` - текущая ошибка
- `connect()` - подключиться к WebSocket
- `disconnect()` - отключиться от WebSocket
- `subscribe(pollId)` - подписаться на обновления опроса
- `unsubscribe(pollId)` - отписаться от обновлений
- `send(message)` - отправить сообщение

**Функции:**

- ✅ Автоматическое подключение при создании
- ✅ Автоматическое переподключение при разрыве соединения
- ✅ Подписка на обновления конкретных опросов
- ✅ Типизированные WebSocket события
- ✅ Автоматическая очистка при размонтировании компонента

**Пример использования:**

```typescript
const { isConnected, subscribe } = useWebSocket({
  pollId: 1,
  onUpdate: results => {
    console.log('New results:', results);
  },
});
```

## Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
VITE_API_URL=http://localhost:3000/api
VITE_WS_URL=ws://localhost:3002
```

## Mock Authentication (Development)

### ⚠️ Важно для тестирования

В dev режиме используется **mock authentication** с фиксированным `user_id = 1`.

**Перед тестированием голосования:**

1. Убедитесь, что в базе данных есть пользователь:

   ```sql
   SELECT * FROM users WHERE id = 1;
   -- Если нет, создайте:
   INSERT INTO users (username, email, password_hash)
   VALUES ('test_user', 'test@example.com', 'mock_hash');
   ```

2. В правом нижнем углу приложения увидите **желтую карточку** с текущим user ID

3. Используйте кнопки или консольные команды для управления:
   ```javascript
   mockAuth.setUserId(2); // Сменить пользователя
   mockAuth.clearAllVoted(); // Очистить голосования
   ```

**📚 Подробнее:**

- [QUICK_START_MOCK_AUTH.md](./QUICK_START_MOCK_AUTH.md) - быстрый старт
- [MOCK_AUTH_GUIDE.md](./MOCK_AUTH_GUIDE.md) - полная документация

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен-сборки
npm run preview

# Проверка типов
npm run type-check

# Линтинг
npm run lint
npm run lint:fix
```

## Технологии

- **Vue 3** - JavaScript-фреймворк
- **TypeScript** - типизация
- **Vue Router** - маршрутизация
- **Axios** - HTTP-клиент
- **WebSocket** - real-time коммуникация
- **Tailwind CSS** - стилизация
- **Vite** - сборщик

## Интеграция с другими сервисами

### API Service

Приложение взаимодействует с `api-service` через REST API:

- `GET /polls` - получение списка опросов
- `GET /polls/:id` - получение конкретного опроса
- `GET /polls/:id/options` - получение вариантов ответов
- `GET /polls/:id/results` - получение результатов
- `POST /votes` - отправка голоса

### Realtime Service

Приложение подключается к `realtime-service` через WebSocket для получения обновлений результатов в реальном времени:

- Подключение: `ws://localhost:3002`
- События: `subscribe`, `unsubscribe`, `poll_update`

## Особенности реализации

1. **Управление пользователями**: В текущей версии используется временный ID пользователя, генерируемый на клиенте и сохраняемый в localStorage. В продакшене следует интегрировать полноценную аутентификацию.

2. **Предотвращение повторного голосования**: Приложение сохраняет информацию о проголосованных опросах в localStorage. После голосования пользователь видит результаты вместо формы голосования.

3. **WebSocket reconnection**: Автоматическое переподключение при потере соединения с настраиваемым интервалом.

4. **Типобезопасность**: Полная типизация всех компонентов, API и WebSocket событий с использованием общих типов из `@shared/types`.
