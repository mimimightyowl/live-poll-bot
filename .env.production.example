# ============================================
# Production Environment Variables
# ============================================
# Copy this file to .env.production and fill in production values
# Used by docker-compose.prod.yml

# PostgreSQL Configuration
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme_strong_password_here
POSTGRES_DB=main

# Telegram Bot
# Get your production token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_production_bot_token_here

# Frontend URL
# Your production domain where poll app is hosted
FRONTEND_URL=https://your-domain.com

# ============================================
# Production Security Notes:
# ============================================
# 1. NEVER commit .env.production to git
# 2. Use strong, unique passwords for production
# 3. Store secrets securely (use secrets management systems)
# 4. Use separate bot token for production
# 5. Configure firewall rules for database access
# 6. Enable SSL/TLS for all external communications
# 7. Regular backup of production database
