# ============================================
# Realtime Service - Production Environment
# ============================================

# Node Environment
NODE_ENV=production

# Server Ports
# WebSocket server port for client connections
WS_PORT=3001

# HTTP API port for inter-service notifications (from api-service)
HTTP_PORT=3002

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://app:secret@db:5432/main

# Alternative: Individual database parameters (if not using DATABASE_URL)
# DB_HOST=db
# DB_PORT=5432
# DB_USER=app
# DB_PASSWORD=secret
# DB_NAME=main

# ============================================
# Production Notes:
# ============================================
# 1. Use strong passwords in production
# 2. Use environment variables or secrets management
# 3. WebSocket URL for frontend: ws://your-domain.com:3001
# 4. HTTP API is internal, should not be exposed publicly
# 5. Consider using reverse proxy (nginx) for SSL/TLS
